import React from 'react';
import ProjectList from '../ProjectList';

function Portfolio() {
    return (
        <div>
            {ProjectList}
            project list
        </div>
    );
}

export default Portfolio;